<div class='container'> 
  <h1>Orders</h1>
  <table>
    <thead>
      <table class="w3-table-all">
      <tr class="w3-light-grey w3-hover-grey">
        <th>Sender Name</th>
        <th>Receiver Name</th>
        <th>From</th>
        <th>To</th>
        <th>Weight KG</th>
        <th>Contact</th>
        <th colspan = '3'>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% @orders.each do |order| %>
        <tr class="w3-hover-white">
          <td> <%=order.sender_name%></td>
          <td> <strong><%= order.receiver_name %></strong> </td>
          <td> <strong><%= order.from %></strong> </td>
          <td> <strong><%= order.to%></strong> </td>
          <td> <%= order.weight_capacity %> </td>
          <td> <strong><%= order.contact %></strong> </td>
          <td><%= link_to 'View',order_url(order), class: 'btn btn-light'%></td>  
          <td><%= link_to 'Delete',order_url(order), method: :delete, class: 'btn btn-danger' %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>